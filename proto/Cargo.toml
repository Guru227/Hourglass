[package]
name = "hourglass-proto"
version = "0.7.0"
edition = "2021"
description = "Types shared by hourglassd (resident daemon) and hourglass-ui (window host)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
dirs = "6"
